interface:
  display_name: "Inventory Project"
  short_description: "Repo-specific patterns and workflows for inventory_project"
  default_prompt: "Use the inventory_project repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true